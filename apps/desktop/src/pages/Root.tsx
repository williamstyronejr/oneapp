import { Providers } from "../components/Providers";

export default function Root() {
  return (
    <Providers>
      <main className="bg-red-500">
        <div>Root PAge</div>
      </main>
    </Providers>
  );
}
